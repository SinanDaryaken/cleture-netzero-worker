{{ __('identity.brand.product') }} / {{ __('identity.brand.company') }}
{{ __('identity.brand.tagline') }}

{{ __('identity.email_verification.title') }}

{{ __('identity.common.greeting', ['name' => $organizationUserName]) }}

{{ __('identity.email_verification.introduction') }}

{{ __('identity.email_verification.action') }}:
{{ $actionUrl }}

{{ __('identity.email_verification.notice') }}
