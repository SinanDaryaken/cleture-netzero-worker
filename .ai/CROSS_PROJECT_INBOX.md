Cleanup verification: full PHPUnit 69 passed /339 assertions; consumer retirement 17 passed /145 assertions. Identity/tenant tasks and stale-execution guards pass. No pending legacy emission task in the central readonly inspection. No runtime restart or DB mutation.

# Cross-Project Instruction Inbox

This file records the review and implementation status of instructions received from other repositories, project tasks, agents, or automations. Every entry remains subject to the cross-project review procedure in `AGENTS.md`.

## Intake procedure

1. Record the source and requested outcome before making changes.
2. Identify the Worker files, database contracts, runtime services, and accepted decisions it could affect.
3. Compare the request with `AGENTS.md`, applicable `.ai/rules`, architecture decisions, installed package versions, the current implementation, and the live schema where relevant.
4. Record the reviewed scope, compatibility conclusion, implementation state, and any conflicts or missing information.
5. Implement compatible, sufficiently specified requests and verify them normally. Leave incompatible or incomplete requests pending with explicit acceptance conditions.
6. Re-evaluate revised requests from the beginning.

## Entries

### 2026-09-03 — MyCleture localized identity-mail contract

- Source: cleture-netzero-my project task `01a06613-d2e2-7671-ab57-f224b3ace399`.
- Requested outcome: Accept identity-mail payload version 2 with `{organizationUserId, locale}`, preserve version 1 fallback behavior, and add Turkish/English mail rendering.
- Affected areas: processing-task contract registry and definitions, identity jobs, mail templates/translations, and processing/job/mail tests.
- Known conflicts: The original request arrived while canonical lifecycle remediation was in progress. The owner has now explicitly approved a backward-compatible implementation: v1 remains ID-only with Turkish fallback, while v2 carries exactly `{organizationUserId, locale}` and keeps the canonical lifecycle.
- Current state: The project owner approved Worker locale-v2 development in the active Worker conversation while MyCleture producer work proceeds separately. Worker now supports v1 ID-only tasks with the Turkish fallback and v2 tasks carrying exactly `{organizationUserId, locale}` for `tr` or `en`. The newly added or changed contract, localization, failure-snapshot, and locale-isolation tests pass with 11 scenarios and 38 assertions. MyCleture must not deploy v2 production until the updated Worker is deployed and restarted.
- Status: Approved, implemented, and focused verification passed.

### 2026-09-03 — MyCleture identity-mail Worker instruction

- Source: cleture-netzero-my project task / delegated Worker work.
- Requested outcome: Add processing-task consumption and identity verification/password-reset mail jobs to the Worker.
- Affected areas: processing-task lifecycle, database queue handoff, retry/failure ownership, mail jobs, scheduler and runtime services.
- Conflict found: The resulting direct `pending -> processing -> queue -> delete` handoff conflicts with NetZeroAdmin ADR-045's `pending -> queued -> processing` lifecycle, dispatch-token fencing, and canonical domain retry ownership.
- Current state: The instruction was implemented before this quarantine procedure existed. Its reusable mail and runtime parts require review; its processing-task lifecycle must not be treated as the Worker baseline.
- Status: Pending owner review and remediation decision.

### 2026-09-03 — Password-recovery security follow-up

- Source: Active cleture-netzero-my security-remediation task requested by the project owner.
- Requested outcome: Before creating a password-reset token, enforce the configured broker cooldown with `recentlyCreatedToken`; add a supported, secret-free `organization-user.password-changed` processing-task contract and localized confirmation mail after a successful reset.
- Affected areas: `app/Jobs/Identity/SendOrganizationUserPasswordReset.php`, identity-mail contract definitions and registry, a new password-changed job/mailable/templates, and their job/mail/processing-task tests.
- Known conflicts: The existing identity rule still applies: tokens and reset links must never enter durable task payloads, failure snapshots, or logs. NetZeroAdmin has no persistent Organization User locale field, so per-user Turkish/English selection cannot be added without separately approving a locale source or contract change; the current Turkish fallback remains authoritative.
- Current state: The project owner approved this entry in the active Worker conversation. Cooldown enforcement and the v1 password-changed Worker contract are implemented. Empty safe failure payloads are stored as JSON objects, the PHPUnit result-cache permission issue is resolved, the focused suite passes with 34 tests and 173 assertions, and the full suite passes with 36 tests and 175 assertions. The current Turkish fallback remains in place; per-user locale selection still belongs to the separately pending locale-contract decision.
- Status: Approved by project owner, implemented, and verified. MyCleture may implement its producer change after the updated Worker is deployed.

### 2026-09-04 — stancl/tenancy dependency installation

- Source: Delegated Worker task from cleture-netzero task `01a06b1f-a669-7283-99c8-d1d3b0405ad9`, with explicit project-owner approval to add the dependency in this repository.
- Requested outcome: Verify and pin a stable `stancl/tenancy` release compatible with the Worker's PHP and Laravel versions, install it through the configured Herd Lite platform runner, and verify package discovery without provisioning tenants.
- Reviewed scope: `AGENTS.md`, `.ai/rules`, the existing processing-task and tenant-ID references, installed PHP/Laravel/package versions, Composer dependency resolution, official package metadata/documentation, `composer.json`, `composer.lock`, and Laravel package discovery.
- Compatibility conclusion: `stancl/tenancy` v3.10.1 supports PHP `^8.0` and Illuminate `^10|^11|^12|^13`; it is compatible with PHP 8.5.0 and Laravel 13.30.1. No authoritative Worker rule conflicts with dependency-only installation.
- Current state: `stancl/tenancy` is pinned to `3.10.1`; its three new transitive packages are locked and installed; platform requirements, lock installability, package discovery, Artisan command registration, and security audit passed. The durable provisioning boundaries were recorded in `.ai/rules/tenancy.md`.
- Explicit exclusions: No tenancy install scaffold was published; no migration, tenant model/domain query, tenant database create/delete, tenant migration, seed/backfill, role/grant, processing-task lifecycle change, credential persistence, or database write was performed.
- Status: Approved by project owner, implemented, and focused verification passed. Provisioning integration remains a separate future Worker task.

### 2026-09-04 — Organization-owned tenant provisioning

- Source: Active cleture-netzero-my conversation, explicitly approved by the project owner after the stancl/tenancy dependency installation.
- Requested outcome: Consume secret-free `tenant.provision` v1 tasks created atomically with organization-owned tenants; provision a deterministic PostgreSQL database without Redis or dirty flags; expose readiness through central tenant state.
- Affected areas: Processing-task contract registry and lifecycle hooks, tenant model/context, provisioner/runtime database connections, tenant migrations, and provisioning tests.
- Compatibility conclusion: The request preserves the canonical pending/queued/processing task lifecycle, UUID tenant identity, database-queue transport, safe failure snapshots, deterministic database naming, and separate provisioner/runtime roles. The obsolete Customer registry is not part of the contract.
- Current state: Worker integration is implemented with `pending -> provisioning -> ready/failed` tenant transitions. Recoverable task failures retain `provisioning`; terminal and expired failures set non-ready tenants to `failed` and keep `active=false`. Provisioning migrations run through the distinct DDL-capable provisioner connection; the applied migration ledger must exactly match the repository's canonical tenant migration set. The runtime role receives DML/default privileges without database ownership, and readiness is verified through a bootstrapped runtime tenant connection that always restores central context. The first canonical tenant schema contains local `users` and an unbounded `organizational_units` tree; the legacy core's separate company/facility tables and triple pivot were reviewed but not copied. Tenant users do not persist email-verification or Laravel remember-me state; the tenant-facing application owns Redis-backed login, session, and access-token state. Existing ready tenants are upgraded separately from `processing_tasks` by the idempotent `tenant-schemas:upgrade` release command. The command selects outdated tenants in bounded chunks, uses global command isolation and per-tenant PostgreSQL advisory locks, runs pending migrations through the provisioner role, repairs runtime grants, verifies the canonical ledger through the runtime connection, updates only successfully verified `schema_version` values, continues across tenant failures, and returns a failing exit code when any tenant fails. Empty failure payloads remain JSON objects. The first real tenant was provisioned and verified through the runtime role. The command tests now execute before their side-effect assertions, and the complete Worker suite passes with 81 tests and 369 assertions.
- Status: Approved by project owner, implemented, runtime-verified, and automated verification passed.

### 2026-09-04 — Tenant provisioning queue runtime follow-up

- Source: Active cleture-netzero-my analysis, explicitly approved by the project owner in the current Worker conversation.
- Requested outcome: Make the platform queue worker consume both `identity-mails` and `tenant-provisioning`, raise its worker timeout to 300 seconds while remaining below the 330-second database queue retry window, recreate only the queue service, and verify the existing provisioning request without creating another request.
- Reviewed scope: Worker processing-task routing, queue retry configuration, transport job timeout, existing processing-task tests, the platform Worker Compose service, platform decision D-018, and the current runtime state of the existing tenant task.
- Compatibility conclusion: `tenant.provision` is already routed to `tenant-provisioning`; the transport job already has a 300-second timeout and `failOnTimeout=true`; 300 seconds remains below `retry_after=330`. The project owner's active instruction explicitly supersedes D-018's previous identity-mail-only queue scope.
- Current state: The Compose command and focused timeout/migration-order regression tests are updated, and the Compose schema is valid. Separate non-superuser `tenant_provisioner` and `tenant_runtime` PostgreSQL roles were configured with secret-backed Worker credentials; only the provisioner can create databases. Runtime verification exposed a Laravel Blueprint command-order defect in the self-referencing organizational-unit migration, so its primary key is now declared explicitly before the foreign key. The existing task then completed through the configured queue: the tenant is `ready`, active, and has a verified schema version; both the central task row and tenant-provisioning transport row were removed. The queue remains running, and no new provisioning request was created. The complete Worker suite passes with 81 tests and 369 assertions.
- Status: Approved, implemented, runtime-verified with the existing provisioning request, and automated verification passed.

### 2026-09-04 — Organization unit types tenant schema

- Source: Active cleture-netzero-my task delegated by the project owner, followed by an explicit instruction to make Worker the sole owner of the migration and remove the source migration from MyCleture.
- Requested outcome: Add a tenant-only `organization_unit_types` table and a nullable, indexed, delete-restricted type reference on `organizational_units`; preserve the existing company/facility structural flags and upgrade every existing ready tenant without data loss.
- Reviewed scope: Worker tenant migration ownership and ordering rules, the canonical migration path, schema-version calculation, isolated tenant upgrade command, source schema proposal, installed Laravel/PostgreSQL capabilities, and the live central/tenant schema.
- Compatibility conclusion: The change is compatible with the unbounded organizational-unit tree and does not alter central schema ownership. A nullable foreign key preserves existing rows. The Worker version adapts the proposal to canonical UUIDv7, normalized-name, and non-negative sort-order constraints, and remains separate from the company/facility structural flags.
- Current state: Worker now owns the Artisan-generated canonical migration. It creates UUIDv7-backed `organization_unit_types` with unique normalized names, active state, non-negative sort order, and timezone timestamps; it adds a nullable indexed UUID foreign key with restricted updates/deletes without changing the company/facility flags. The focused PostgreSQL migration-definition test passes with 2 tests and 14 assertions. The isolated release command upgraded tenant `01a06bc0-326a-700a-a2d7-ec6065b8f6d6`, preserving all 5 existing organizational-unit rows, and runtime/schema verification passed. The central database has neither the tenant table nor the tenant column. A second release-command run reported 0 upgrades required and 1 tenant already current.
- Status: Approved, implemented, focused verification passed, and every existing ready tenant was upgraded successfully.

### 2026-09-04 — Atlas–ADEME candidate-package ingest v1

- Source: NetZeroAdmin task `01a06b1f-a669-7283-99c8-d1d3b0405ad9`, delegated to this Worker with explicit project-owner approval for the first ingest slice and the S3 Flysystem adapter.
- Requested outcome: Register and consume the exact secret-free `emission.candidate.ingest@v1` task; stream the version-pinned S3-compatible artifact; fail closed on size, media type, checksum, manifest, archive, member digest, and NDJSON envelope defects while preserving the canonical processing-task claim, lease, retry, failure-snapshot, and dispatch-fencing lifecycle.
- Reviewed scope: Worker processing-task contracts and lifecycle, applicable shared rules, installed Laravel/Flysystem versions, Admin INT-001 task payload and central package/member schema, Admin manifest and candidate-staging contracts, and local object-storage/deployment boundaries. Live central-schema inspection was unavailable because the Boost process could not resolve the configured PostgreSQL host, so the accepted Admin migrations, models, actions, and tests were used as the schema authority.
- Compatibility conclusion: The v1 payload and read-only artifact verification fit the current Worker architecture without a migration. Laravel Filesystem provides a provider-neutral S3-compatible boundary for local MinIO and non-local Huawei OBS. Object-store transport failures remain retryable; immutable contract and integrity defects use the existing permanent-failure taxonomy. Credentials, buckets, policies, and runtime services remain deployment-owned.
- Current state: `league/flysystem-aws-s3-v3` is installed; `emission.candidate.ingest@v1` is registered on its own queue; storage endpoints and credentials are environment-driven; object reads are streamed and object-version pinned when supplied; the registered package/manifest/member envelope, archive allow-list, compressed and expanded sizes, artifact/member hashes, record counts, and bounded JSON-object lines are verified. Tests cover verification, checksum/manifest/member/schema failures, missing and transient object storage, unsupported versions, queue routing, stale dispatch fencing, and both versioned and unversioned storage reads. The complete Worker suite passes with 81 tests and 369 assertions.
- Explicit boundary: Admin's complete candidate validation and canonical bulk-write contract is not present in Worker and no additional ownership or dependency was approved. A fully verified artifact is therefore archived with the safe permanent code `candidate_staging_contract_requires_decision`, while the package remains `received`; Worker does not falsely mark it `staged` or invent canonical writes.
- Status: Approved and implemented through verified artifact staging boundary; automated verification passed. Deployment still must provide read-only object-store configuration and subscribe the runtime worker to `emission-candidate-ingest`.

### 2026-09-05 — Atlas main 35b82db / Admin V2 durable staging coordination

- Source: Atlas main commit `35b82db` follow-up delegated from project task `01a06d80-9ff9-76e0-987b-438ea57810bb` at the project owner's explicit request for record-only intake.
- Related entry: `2026-09-04 — Atlas–ADEME candidate-package ingest v1` above. That completed v1 verification boundary must not be reimplemented or treated as authorization for a v1 downgrade.
- Pending dependency: Atlas will develop independent validation and package orchestration after `35b82db`. Worker coordination requires the Admin V2 candidate-intake contract, a compatible durable staging writer, and idempotent full-snapshot staging proof.
- Required contract coverage: Define the entity, findings, and source-diff staging behavior; define the policy for a zero-record relationships member; and preserve explicit retry behavior for transient failures plus permanent handling for validation failures.
- Ownership boundary: Candidate acceptance is not publication. The canonical publish decision remains Admin-owned.
- Delivery gate: Atlas delivery must remain closed until Admin V2 intake and Worker durable staging are verified together. No V1 downgrade is permitted.
- Review/update (2026-09-07): Admin task `01a07b2f-f200-7d50-825b-38876f59228f` requested V2 durable staging. Read-only review of Admin HEAD `83750cd7351e9e48cf647acc4b8bb4de3005f76d` plus its working tree found missing validation-sharing, advisory storage, completion receipt, and release-pin contracts. The project owner subsequently explicitly approved Worker development in the active Worker conversation and separately approved adding Opis JSON Schema.
- Implemented scope: Independent V2 artifact verification only. Opis `2.6.0` consumes local copies of the five sealed Admin schemas and their snapshot manifest, with pinned file hashes and preserved raw JSON object types. The schema source commit is `d5c761c8d6c151ff548ba33f9ee0824138ed07a4`; manifest SHA-256 is `1990bcdfe11133e0448d64af07d6790ccec594ffae8089b8615effc45d43f1fc`. Existing archive/member byte, size, hash, and count checks now cover exact V2; empty relationships, entity record hashes, package candidate/semantic identities, entity evidence/provenance/mapping references, raw-asset pins, advisory candidate references, diff identity/hash classification, and actual summary counts are checked. A Worker-local 100000-record budget bounds the temporary in-memory identity index; this is not an Admin transport-limit decision.
- Remaining acceptance boundary: This is not complete Admin application validation or full previous-package snapshot continuity proof. Trusted catalog/blocker validation sharing, source-diff durable representation, advisory finding persistence/replay integration, four-member completion receipt, durable retry/crash recovery, and final staged transition remain pending. Admin's later advisory-finding mapping update was received as informational only and has not been integrated. A verified package still ends at `candidate_staging_contract_requires_decision` and remains `received`; no staging, review decision, approval, publication, or tenant write occurs.
- Runtime verification: Boost's live central query failed because `postgres` could not resolve; Batch 37/38 were not independently verified through Worker. No development DDL/DML, migration, runtime lifecycle, rollout, commit, or push was performed for this slice.
- Verification: Candidate ingest plus related processing-task executor/consumer/transport/storage tests pass with 55 tests / 255 assertions, using isolated SQLite memory fixtures and an in-process artifact stream fake. Opis validation, ZIP parsing, and application processing-task paths are real; no external object store or PostgreSQL staging write was exercised. Pint and diff checks passed; Composer reported no security advisories.
- Status: Owner-approved verifier slice implemented and focused verification passed. Durable staging and rollout remain pending (BEKLEYEN).

### 2026-09-07 — Admin Worker record-validation service client

- Source: Admin task `01a07b2f-f200-7d50-825b-38876f59228f` delivered its record-validation HTTP contract; the project owner explicitly approved Worker implementation in the active Worker conversation after the read-only compatibility review.
- Reviewed scope: Admin's current `WorkerValidationContract`, `WorkerValidationRuleset`, `ValidateWorkerCandidateBatch`, authentication middleware/controller, sealed request/response schemas, and delivery notes; Worker emission/task rules, installed Laravel 13.30.1 and Opis 2.6.0 APIs, existing verifier and retry behavior. No Admin runtime code is copied or included across repositories.
- Contract pins: request SHA-256 `2baf4af87522f4506a1a243f3298a134021cee85bb74cd306427a7c9a63c3cba`; response SHA-256 `d222a39f4ac5d6b99895a15397a093d74fc4bae914543b6f78bc8517d5702de1`. Exact local schema copies are hash checked. The deployment validator SHA is an explicit configuration prerequisite, not automatically discovered or repinned from a 409 response.
- Implementation: `AdminCandidateValidationClient` uses a separately configured HTTPS origin/key/secret, request UUID and fresh nonce, bounded canonical request batches (25 records, 1 MiB record JSON and 4 MiB actual encoded body), and a bounded streamed response. It validates response schema/canonical bytes/hash/HMAC, all request/package/manifest/artifact/validator bindings, results hash, one-to-one line/input hashes, status/issue consistency and checked-at freshness. Service wire JSON encoding remains separate from candidate record hash encoding. Redirects/TLS bypass are disabled. Network/429/server failures use the existing task retry lifecycle; 409 never repins; invalid or rejected responses fail closed with safe local errors.
- Integration: When explicitly enabled, Admin validation reads the same temporary ZIP only after all local archive/member/integrity checks finish, preserving actual entity line coordinates. Package-fatal results enter the permanent task failure path. Blocked/valid responses remain transient and are not persisted as findings or receipts; successful traversal still reaches `candidate_staging_contract_requires_decision`, with the package `received`. V1 never calls the new service. The client limits one traversal to a 120-second budget and each request to at most 30 seconds; large packages still require operational throughput/lease and durable continuation design before rollout.
- Configuration: `WORKER_ADMIN_VALIDATION_ENABLED` defaults false; `WORKER_ADMIN_VALIDATION_ORIGIN`, `WORKER_ADMIN_VALIDATION_KEY_ID`, `WORKER_ADMIN_VALIDATION_SECRET`, and `WORKER_ADMIN_VALIDATION_SHA256` must be supplied through deployment. No real credentials, environment values, Admin readiness flags or runtime services were changed.
- Verification: 64 related Worker tests / 314 assertions passed, including HTTP signature/binding tampering, byte/count batching, wrong-pin 409, fresh-nonce retry, TLS configuration, response limits, blocked/package-fatal ingest behavior and V1/task regressions. Pint passed. HTTP is faked and the ingest registry is isolated SQLite memory; this is not real HTTPS/PostgreSQL/S3 interoperability or durable staging proof.
- Remaining: Coordinated immutable deployment descriptor and credentials/source grants; real HTTPS integration; advisory finding/source-diff persistence and replay, full previous-package continuity, four-member completion receipt, crash recovery and final staged transition. No commit/push, rollout, DDL or development DML was performed.
- Status: Owner-approved HTTP client and optional verifier integration implemented and locally verified; runtime enablement and durable staging remain pending.

### 2026-09-07 — Reviewed claim-bound V2 staging adapter (disabled rollout)

- Source: Admin task `01a07b2f-f200-7d50-825b-38876f59228f`, revised staging delivery and explicit default-disabled implementation scope after Worker read-only review. The first draft lacked a prefix digest and a bounded completion acceptance story; the revised immutable prefix chain closes the evidence gap, while full-size throughput remains a rollout prerequisite. Worker rules, existing lifecycle, Laravel 13.30.1 / Opis 2.6.0, Admin actions/controller/authentication and both proposed central migrations were inspected before implementation. Boost returned no processing-task tables; this is not evidence that the new migrations are live.
- Pinned local schemas: request `be3f6d29052f5b8f20f698ce227ce9b4b23d349abf88aa8a9e9f480403eb20d4`, response `9953cea2693cfb0b3f490bb0dc11dd796bfc63cfd5fc61974f516e9e169fd801`, receipt `cd4e4611a19d00747bb582a073a3b33a6923147b0de3f3a1336138842fa3b968`. Admin implementation descriptor remains separately configured; a reported working-tree hash is not automatically a deployment pin.
- Implemented: status-first claim-bound HTTPS staging; raw LF rows with exact encoded-body/count limits; schema/canonical-body/HMAC/request/claim/pin/progress/receipt checks; immutable member prefix chain verification against the verified temporary ZIP; entities then findings/source-diff; exact receipt member hash/size/count binding. Existing receipts finish the task without reopening storage or calling record-only validation. Worker performs no candidate DML; Admin owns candidate persistence and staged transition.
- Lifecycle: immutable execution context is built from actual newly claimed values. Only the registered V2 pointer and enabled staging capability use failure_attempts; attempts remains monotonic. Confirmed new durable rows yield pending with continuation_count increment, cleared claim, no completion/failure hooks. Real failures, expired claims and exhausted queued transports charge once under the current lock; stale tokens/attempts and active-claim transport callbacks do not mutate a newer execution. V1/identity/tenancy retain their existing retry selection.
- Budgets: monotonic whole-attempt budget defaults to 270 seconds, bounded by remaining lease minus safety. Status/append require 30 seconds plus reserve; complete requires 120 seconds plus reserve. No verified new progress means a real bounded failure, never an infinite healthy yield. Synchronous queue dispatch does not return domain outcomes, so explicitly continuable execution uses Bus::dispatchNow inside the already-running durable transport job; other jobs keep dispatchSync.
- Configuration: WORKER_ADMIN_STAGING_ENABLED defaults false. Shared Admin origin/key/secret/validator SHA use existing WORKER_ADMIN_VALIDATION_* settings; WORKER_ADMIN_VALIDATION_CA_BUNDLE optionally selects a readable PEM file for only the Admin clients, otherwise system TLS trust is used. TLS bypass is rejected. Existing candidate_ingress allow-listed environment variables remain the credential interface; no new file-secret loader or dependency was added.
- Verification: 86 processing-task/HTTP/storage tests with 772 assertions and 14 tenancy/job regressions with 55 assertions passed. Includes eight healthy real-append continuations, lost append and complete replies, receipt replay without storage, prefix/signature/claim/policy/receipt corruption, framing, no-progress exhaustion, scheduler/transport failure fencing and CA configuration. Pint and git diff --check passed. Tests use fake HTTP and isolated SQLite, not Admin's benchmark database or a real end-to-end service.
- Remaining rollout conditions: Admin-owned staging evidence and continuation migrations applied and independently verified; final runtime implementation SHA and matching service credentials/source grants; stable Admin DNS and scoped public-CA mount; version-pinned candidate object and least-privilege storage credentials; real HTTPS/PostgreSQL/S3 acceptance and capacity evidence before queue subscription/enablement. No runtime, environment, central migration, external repository, commit or push changes were made for this slice.
- Status: Disabled Worker implementation and local verification complete; runtime acceptance remains pending.

### 2026-09-07 — Live Admin staging migration and descriptor verification

- Source: Final delivery from Admin task `01a07b2f-f200-7d50-825b-38876f59228f`. Read-only review of the final migration sources confirms the entity raw-payload/member-line binding to the canonical record and database-derived semantic identity, preserving the existing wire schema and Worker ownership boundary.
- Independently verified from `cleture-netzero-worker-php-1` (PHP 8.5.8), through the configured central connection: both `2026_09_07_154112_create_candidate_staging_evidence_tables` and `2026_09_07_154224_add_continuation_counters_to_processing_tasks` are recorded in Batch 39. `failure_attempts` and `continuation_count` are non-null integers defaulting to zero, with the nonnegative/continuation_count <= attempts constraint. Staging prefix/cumulative-byte/raw-line/semantic columns and receipt bytes/hash columns exist. All four named staging parent/completion/immutability triggers are enabled. No role names or credential values were output.
- Boost's central query still failed to resolve postgres outside the runtime container; the container query above supplied the live evidence. Only SELECT queries were run; Worker did not apply migrations or change data.
- Independently verified using Admin's read-only `worker:validation-contract` command in its real PHP container: implementation SHA-256 `7d2fe3e15f76bb1bb50980e84717ed4670d5c029eb5caac33a4a9b3bb6371af5`. All three staging schema hashes match the local Worker copies listed above. This is a runtime implementation descriptor, not a Git commit or an automatic configuration update.
- Admin reports 73 related tests / 363 assertions and a 100000-entity plus 100000-diff, 2147400000-raw-byte completion benchmark of 22.074 seconds. Those Admin test runs were not rerun by Worker. The benchmark covers Admin completion only; Worker download, ZIP verification and end-to-end throughput remain separate acceptance gates.
- Status: The live migration-existence/column and current descriptor checks are satisfied. Worker runtime staging remains false. HTTPS/S3 credential pairing, DNS/CA transport, version-pinned object access, end-to-end acceptance and rollout approval remain pending. Only this delivery record changed; no runtime/env/Git or dependency change was made, and no PHP retesting was needed for the documentation-only update.

### 2026-09-07 — Shared Admin request pacing and bounded backpressure

- Source: Admin task `01a07b2f-f200-7d50-825b-38876f59228f` identified the shared IP-based 60/minute validation/staging route limit. Read-only source review confirmed it; blindly charging every expected 429 as a transport failure could exhaust a large package's retry budget. Reviewed against Worker lease, durable-progress and default-disabled boundaries before editing.
- Implemented: Both clients reserve from one origin-scoped atomic-cache quota (also shared across service keys), conservatively capped at 55 reservations in a 61-second sliding window. Status, append, complete and readonly validation all participate. Waits are in one-second increments, outside cache locks, only when the remaining attempt/lease budget still covers the full HTTP reserve. Staging uses monotonic elapsed time and also rechecks absolute lease expiry; readonly traversal now uses a monotonic deadline.
- Valid Retry-After values (bounded 1–300 seconds, integer or exact HTTP date) publish a shared cooldown. The refused operation is not immediately retried. Only previously verified new durable rows permit a healthy continuation on pacing/cooldown deferral; no-progress and malformed Retry-After remain bounded real failures. No server quota increase, schema change, or dependency change.
- Configuration: optional WORKER_ADMIN_LIMITER_CACHE_STORE selects the shared store, otherwise cache.default. Redis/database/memcached are accepted; process-local array is allowed only in automated tests. All Worker processes must share the same cache namespace/store for real quota enforcement; live shared-cache connectivity is an additional rollout check.
- Verification: 95 related processing-task/HTTP/storage tests / 1017 assertions passed, with Pint and diffcheck clean. Tests cover two consecutive quota windows, independent limiter instances, readonly participation, eight quota-limited progress turns without failure-budget consumption, shared Retry-After delay/recovery, invalid header failure, expired lease and bounded no-progress exhaustion. HTTP, sleep and time are faked and cache uses isolated array in tests; this does not prove distributed Redis behavior or end-to-end throughput.
- Status: Disabled pacing implementation locally verified. Runtime/env/Git state and Admin code/pin remain unchanged; no live services were operated.

### 2026-09-07 — Disabled Admin identity pairing

- Source: Admin task `01a07b2f-f200-7d50-825b-38876f59228f`, owner-scoped auth/source/pin preparation independently of Docker rollout. Read-only review confirmed the owner-only delivery file, TEST_SOURCE-only scope, disabled flags, current verified implementation pin and absence of a conflicting Worker identity.
- Applied only the five allow-listed WORKER_ADMIN_VALIDATION origin/key/secret/SHA/CA settings and the two explicitly false Worker enable flags to the ignored Worker .env, using an atomic replacement with existing UID/GID/mode and unrelated configuration retained. No credential contents appeared in commands, outputs, messages or this record. No S3 credentials were supplied or changed.
- Verification: The project's actual Dotenv parser successfully parsed both configurations and confirmed key/secret equality, origin/pin/CA equality with delivery, TEST_SOURCE-only Admin scope and both applications' disabled flags. Ownership and mode were verified unchanged. The initial unrelated-line diagnostic used newline-normalizing text reads against CRLF bytes and returned a false mismatch; the writer retained original unrelated line bytes, and subsequent parser verification succeeded.
- Status: Disabled file-level identity pairing complete. No configuration-cache operation, queue/consumer start, Docker lifecycle, live HTTP request or runtime enablement occurred. Future network acceptance uses the platform-owned netzero-candidate-ingress network, as reported by Admin; Worker did not change platform files. Shared cache, CA mount/DNS and S3/versioned-object/end-to-end acceptance remain rollout conditions.

### 2026-09-07 — Real synthetic Worker acceptance completed

- Source: Coordinated owner-approved activation through Admin task `01a07b2f-f200-7d50-825b-38876f59228f`. Worker atomically paired the exact-object child S3 credentials and configured staging=true/readonly-validation=false; unrelated environment lines and ownership/mode were preserved. Admin coordinated queue recreation with candidate subscription and scheduler refresh, then registered the exact synthetic TEST_SOURCE manifest. Worker did not run a manual consumer or operate lifecycle services.
- Independent preflight: Real Worker provider read VersionId `d61c8fb0-e4ec-47e9-a136-b17bbabc3b9d` successfully, obtaining 6934 bytes with SHA-256 `09e3f18ccd295c936a37032078caefeddc9948f61af5c9fbdc59e360337e6dff`. Admin runtime descriptor remained `7d2fe3e15f76bb1bb50980e84717ed4670d5c029eb5caac33a4a9b3bb6371af5`; scoped CA/DNS and Redis data/lock connectivity were checked independently. Credential values were never output.
- Independent completion evidence: Package `01a07cd6-514c-71d3-9c45-845035005991` is staged. Task `01a07cd6-5161-7072-a7d7-7052b585ac18` is deleted, has no failure snapshot and no remaining matching candidate queue job. Exactly one immutable receipt and five raw staging rows exist. All four receipt member hashes/counts/byte sizes match the registered manifest: entities 1/4423 bytes, findings 3/1274 bytes, source_diff 1/775 bytes, relationships 0/0 bytes. Receipt hash, task/pin bindings, manifest transport SHA `3af77be6b063fc512b73e1e4d2ec269b01f48475d939af9f1014d3005572eac1`, artifact SHA and VersionId match.
- Boundaries: approval_allowed=false, publish_allowed=false, source_comparison_semantics_verified=false. This proves the real small synthetic normal flow, not scientific approval, full-size Worker throughput, or a real lost-HTTP-response replay. Lost-response/new-claim recovery remains separately covered by automated tests; Admin additionally reports five successful staging POST responses and real repeated Register-usecase intake returning created=false, the same package/receipt, no new task or records, and zero review decisions; that intake replay was run by Admin, not independently rerun by Worker. The exact-object child credentials expire at 2026-09-08T17:01:33Z and must not be treated as permanent deployment credentials. Runtime scope remains TEST_SOURCE only, with no production ADEME activation.

## Entry template

### YYYY-MM-DD — Short title

- Source:
- Requested outcome:
- Affected areas:
- Known conflicts:
- Status: Pending owner review.


### 2026-09-08 — Owner-approved Atlas → Worker → Admin transport cleanup
- Source: Project owner in shared Admin task, explicitly requesting sequential cleanup and all project tests.
- Reviewed scope: Emission ingest definition/job, artifact verification, Admin clients, continuation-only additions to the shared consumer/executor, dedicated tests and configuration.
- Decision: Retire emission.candidate.ingest. Preserve identity/tenant contracts, retry lifecycle, dispatch-token and execution-attempt fencing. Unsupported legacy tasks remain undispatched; no queue/task rows or schema are deleted.
- Status: Implemented; verification pending. No dependency removal, queue service restart, or central database mutation.
